name: verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  plugin-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "20"
      - name: Portable package, protocol, privacy, and skills
        run: npm test
      - name: Public API smoke tests (network-dependent)
        continue-on-error: true
        run: npm run test:live || (sleep 10 && npm run test:live)
